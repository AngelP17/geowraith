/** GeoCLIP embedding size for both vision and location encoders. */
export const FEATURE_VECTOR_SIZE = 512;

/** Coordinate catalog size sampled from GeoCLIP 100K gallery for local indexing. */
export const GEOCLIP_REFERENCE_COORDINATES = 50000;
